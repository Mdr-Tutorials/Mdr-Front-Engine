# MdrFrontEngine (MFE)

---

开源可视化前端开发平台 - 从设计到部署的全流程解决方案  
MIT License | 支持本地运行 + 云端协作

口号：**结合蓝图、节点图和代码；打通设计、开发、测试、构建和部署；跨领域开发前端、快速开发 MVP 和学习前端的优质选择。**

我们主张 **"Visualize Beyond Design" (可视化的不仅是设计)**。通过 MFE，你可以将复杂的业务逻辑、数据流以及跨框架的代码生成能力，通过“炼化”过程瞬间转化为生产环境可用的代码。

---

## 核心特性

* **可视化逻辑编排 (Node Graph)**：像连线一样编写业务逻辑，支持内置节点、自定义 JS 脚本与 AI 助手集成。
* **多目标代码炼化 (Code Transmutation)**：一键导出 Vue 3、React、Angular、SolidJS 甚至是原生 HTML 或 htmx 代码。
* **数据驱动引擎**：内置 JSON Schema 定义与动态绑定，实现“数据获取 → 渲染 → 更新”的自动化闭环。
* **全栈生态支持**：原生配套 CLI 命令行工具与 VSCode 插件，打通可视化环境与本地开发工作流。

---

## 📂 项目结构 (Monorepo)

本项目采用 **Turbo + pnpm workspaces** 构建，实现了前后端、文档与工具链的深度解耦：

```text
.
├── apps/
│   ├── web/          # MFE 核心可视化编辑器 (Frontend Engine)
│   ├── backend/      # Go 驱动的后端服务 (Go Service)
│   ├── cli/          # Mdr 命令行工具 (The Mdr CLI)
│   ├── vscode/       # VSCode 插件，连接本地与云端环境
│   └── docs/         # 基于 MFE 构建的官方文档
├── packages/
│   └── ui/           # 共享的组件库与 Storybook 预览
└── turbo.json        # Turbo 任务调度配置
```

---

## 🚀 快速开始

### 前提条件

* **pnpm** (推荐 v10+)
* **Go** (用于运行后端服务)
* **Node.js** (LTS 版本)

### 安装

```bash
pnpm install
```

### 开发环境启动

启动全栈开发环境（Web + Backend + Docs）：

```bash
pnpm dev
```

---

## 🛠️ 命令指南

本项目通过 `turbo` 进行任务编排，支持过滤（Filter）特定模块运行。

### 1. 全域命令

| 命令          | 说明                                            |
| :------------ | :---------------------------------------------- |
| `pnpm dev`    | **梭哈模式**：并行启动所有应用的开发服务器      |
| `pnpm build`  | 执行全链路构建，产出生产环境产物                |
| `pnpm format` | 统一格式化代码 (TS, TSX, JS, JSX, JSON, MD, GO) |
| `pnpm test`   | 运行全仓库单元测试                              |

### 2. 子应用开发

如果你只想专注于某个模块，可以使用以下命令：

| 模块            | 命令               |
| :-------------- | :----------------- |
| **Web 编辑器**  | `pnpm dev:web`     |
| **Go 后端**     | `pnpm dev:backend` |
| **CLI 调试**    | `pnpm dev:cli`     |
| **文档中心**    | `pnpm dev:docs`    |
| **VSCode 插件** | `pnpm dev:vscode`  |

### 3. 组件驱动开发 (Storybook)

在 `packages/ui` 中独立预览和测试 UI 组件：

```bash
pnpm storybook:ui
```

### 4. 命令行交互 (CLI)

直接运行本地 Mdr CLI 工具进行项目初始化或代码同步：

```bash
pnpm cli [command]
```

---

## 🔬 技术栈

* **Monorepo 管理**: [Turbo](https://turbo.build/) + [pnpm](https://pnpm.io/)
* **Web 核心**: React / Vue (取决于具体 package 配置) + Vite
* **后端引擎**: [Go](https://go.dev/) (高效、稳定、极致并发)
* **逻辑引擎**: 节点图系统 (Node-based Graph)
* **质量保证**: Prettier + Husky (Git Hooks)

---

## 📜 许可证

本项目基于 **MIT** 许可证开源。

---

> **MdrFrontEngine** —— 打破边界，让灵感在代码中自由跳动。
> Minsecrus 2026.2.2
