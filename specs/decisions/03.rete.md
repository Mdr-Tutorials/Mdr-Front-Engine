# [未来项] 使用 Rete.js 作为节点图编辑器

## 状态

- Future（Parking Lot）
- 日期：2026-02-08
- 触发条件：Workspace 重构主线完成后再评估立项

## 决策结论（未来方向）

Rete.js 保留为 NodeGraph 编辑器的候选方向之一，用于后续阶段评估与 PoC。

当前阶段不进入实现，不纳入迭代承诺。

## 本期边界

1. 不实现 NodeGraph 编辑器 UI。
2. 仅保留协议占位能力（见 `specs/decisions/12.intent-command-extension.md` 中 `core.nodegraph.*`）。
3. 不引入 Rete.js 运行时依赖到主交付链路。

## 后续评估入口

当满足以下条件后再启动正式 ADR：

1. Workspace/VFS + Command + Sync 主线稳定上线。
2. 路由与文档模型冻结到可承载 NodeGraph 互操作。
3. 明确 NodeGraph MVP 范围、性能预算与测试基线。

## 备注

本文档用于保留技术方向，不代表当前版本交付承诺。
