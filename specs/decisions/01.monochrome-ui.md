# 单色 UI 设计系统

## 决策背景

在 MDR Front Engine 项目中，我们需要建立一个统一、现代、可扩展的 UI 设计系统。经过对多种设计方向的调研，我们决定采用 **单色设计系统（Monochrome Design System）**，基于以下考虑：

### 设计目标

1. **专注内容** - 减少视觉干扰，让用户专注于核心功能
2. **现代简约** - 符合现代设计趋势，界面清爽
3. **可扩展性** - 易于扩展新的颜色和功能
4. **可访问性** - 确保良好的对比度和可读性
5. **开发友好** - 便于开发和维护

## 单色设计系统概述

### 核心原则

#### 1. 色彩哲学
采用单色调色板，通过明度、饱和度的变化来创建层次和重点：

```scss
// 核心灰度系统
$colors: (
  0: #FFFFFF,      // 纯白
  1: #F5F5F5,      // 极浅灰
  2: #EEEEEE,      // 浅灰
  3: #E0E0E0,      // 中浅灰
  4: #CCCCCC,      // 中等灰
  5: #999999,      // 中灰
  6: #666666,      // 深灰
  7: #3f3f3f,      // 中深灰
  8: #1A1A1A,      // 深灰
  9: #0D0D0D,      // 极深灰
  10: #000000      // 纯黑
);
```

#### 2. 语义化颜色
在灰度基础上，引入功能性颜色：

```scss
// 功能颜色系统
$semantic-colors: (
  // 状态颜色
  danger: #ff6265,
  danger-hover: #D9363E,
  danger-subtle: #ff8080,
  danger-subtle-hover: #D9363E23,
  
  warning: #ffb007,
  warning-hover: #eb9901,
  subwarning: #ffb728,
  danger-subtle-hover: #e08e0027,
  
  // 交互颜色
  primary: #3f3f3f,
  primary-hover: #1A1A1A,
  secondary: #666666,
  secondary-hover: #3f3f3f,
  
  // 边框颜色
  border-light: #E0E0E0,
  border-medium: #CCCCCC,
  border-dark: #999999
);
```

### 设计规范

#### 1. 层次结构
通过颜色深浅建立清晰的视觉层次：

```scss
// 背景层次
.surface-primary { background-color: var(--color-0); }      // 主背景
.surface-secondary { background-color: var(--color-1); }  // 次要背景
.surface-tertiary { background-color: var(--color-2); }    // 三级背景

// 文字层次
.text-primary { color: var(--color-10); }      // 主要文字
.text-secondary { color: var(--color-7); }     // 次要文字
.text-tertiary { color: var(--color-6); }     // 辅助文字
.text-disabled { color: var(--color-5); }      // 禁用文字

// 边框层次
.border-light { border-color: var(--color-3); }
.border-medium { border-color: var(--color-4); }
.border-dark { border-color: var(--color-5); }
```

#### 2. 间距系统
基于 4px 网格系统：

```scss
$spacing: (
  xs: 4px,    // 4px
  sm: 8px,    // 8px
  md: 16px,   // 16px
  lg: 24px,   // 24px
  xl: 32px,   // 32px
  xxl: 48px,  // 48px
  xxxl: 64px  // 64px
);
```

#### 3. 圆角系统
```scss
$radius: (
  none: 0,
  sm: 2px,    // 小圆角
  md: 4px,    // 中等圆角
  lg: 8px,    // 大圆角
  xl: 16px,   // 超大圆角
  full: 50%   // 完全圆角
);
```

## 技术实现

### 1. CSS 变量系统

```scss
// 全局 CSS 变量
:root {
  // 基础颜色
  @each $key, $value in $colors {
    --color-#{$key}: #{$value};
  }
  
  // 语义颜色
  @each $key, $value in $semantic-colors {
    --color-#{$key}: #{$value};
  }
  
  // 间距
  @each $key, $value in $spacing {
    --spacing-#{$key}: #{$value};
  }
  
  // 圆角
  @each $key, $value in $radius {
    --radius-#{$key}: #{$value};
  }
  
  // 阴影
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
  
  // 过渡
  --transition-fast: 150ms ease-in-out;
  --transition-normal: 250ms ease-in-out;
  --transition-slow: 350ms ease-in-out;
}

// 暗色模式
[data-theme="dark"] {
  // 反转颜色系统
  --color-0: #000000;
  --color-1: #0D0D0D;
  --color-2: #1A1A1A;
  // ... 其他颜色反转
}
```

### 2. 组件设计模式

#### 基础按钮组件
```scss
// Button.scss
.MdrButton {
  // 基础样式
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-sm) var(--spacing-md);
  border: 1px solid transparent;
  border-radius: var(--radius-md);
  font-family: inherit;
  font-size: 14px;
  font-weight: 500;
  line-height: 1.5;
  text-decoration: none;
  cursor: pointer;
  transition: all var(--transition-fast);
  
  // 尺寸变体
  &.Tiny {
    padding: 2px var(--spacing-sm);
    font-size: 12px;
  }
  
  &.Small {
    padding: var(--spacing-xs) var(--spacing-sm);
    font-size: 13px;
  }
  
  &.Medium {
    padding: var(--spacing-sm) var(--spacing-md);
    font-size: 14px;
  }
  
  &.Big {
    padding: var(--spacing-md) var(--spacing-lg);
    font-size: 16px;
  }
  
  // 类型变体
  &.Primary {
    background-color: var(--color-8);
    color: var(--color-0);
    
    &:hover:not(.Disabled) {
      background-color: var(--color-10);
    }
  }
  
  &.Secondary {
    background-color: var(--color-2);
    color: var(--color-8);
    border-color: var(--color-4);
    
    &:hover:not(.Disabled) {
      background-color: var(--color-3);
    }
  }
  
  &.Ghost {
    background-color: transparent;
    color: var(--color-7);
    border-color: var(--color-4);
    
    &:hover:not(.Disabled) {
      background-color: var(--color-1);
    }
  }
  
  // 状态变体
  &.Disabled {
    opacity: 0.5;
    cursor: not-allowed;
    pointer-events: none;
  }
}
```

#### 导航组件
```scss
// Navigation.scss
.MdrNav {
  display: flex;
  align-items: center;
  padding: var(--spacing-md) var(--spacing-lg);
  background-color: var(--color-0);
  border-bottom: 1px solid var(--color-3);
  
  // 布局变体
  &.Columns-2 {
    justify-content: space-between;
  }
  
  &.Columns-3 {
    .MdrNavLeft { flex: 1; }
    .MdrNavCenter { flex: 1; justify-content: center; }
    .MdrNavRight { flex: 1; justify-content: flex-end; }
  }
  
  // 背景样式
  &.Transparent {
    background-color: transparent;
  }
  
  &.Blurred {
    background-color: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(10px);
  }
  
  // 特殊状态
  &.Float {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
  }
  
  &.CanHide {
    transition: transform var(--transition-normal);
    
    &.Hidden {
      transform: translateY(-100%);
    }
  }
}
```

### 3. React 组件集成

```typescript
// ThemeProvider.tsx
import React, { createContext, useContext, useEffect } from 'react';

interface ThemeContextType {
  theme: 'light' | 'dark';
  setTheme: (theme: 'light' | 'dark') => void;
}

const ThemeContext = createContext<ThemeContextType | undefined>(undefined);

export const ThemeProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [theme, setTheme] = React.useState<'light' | 'dark'>('light');
  
  useEffect(() => {
    document.documentElement.setAttribute('data-theme', theme);
  }, [theme]);
  
  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};

export const useTheme = () => {
  const context = useContext(ThemeContext);
  if (!context) {
    throw new Error('useTheme must be used within ThemeProvider');
  }
  return context;
};
```

## 设计优势

### 1. 视觉一致性
- 统一的颜色系统确保所有组件视觉一致
- 标准化的间距和尺寸系统
- 一致的交互反馈

### 2. 开发效率
- CSS 变量系统便于主题切换
- 语义化的类名易于理解和使用
- 模块化的样式结构便于维护

### 3. 可访问性
- 良好的颜色对比度
- 支持键盘导航
- 适配屏幕阅读器

### 4. 扩展性
- 易于添加新的颜色变体
- 支持自定义主题
- 灵活的组件组合

## 使用示例

### 基础布局
```tsx
import { MdrButton, MdrNav, MdrLink } from '@mdr/ui';

function App() {
  return (
    <div className="app">
      <MdrNav>
        <MdrNav.Left>
          <MdrLink to="/" text="Logo" />
        </MdrNav.Left>
        <MdrNav.Center>
          <MdrLink to="/products" text="Products" />
          <MdrLink to="/about" text="About" />
        </MdrNav.Center>
        <MdrNav.Right>
          <MdrButton text="Sign In" category="Secondary" />
          <MdrButton text="Get Started" category="Primary" />
        </MdrNav.Right>
      </MdrNav>
      
      <main className="main-content">
        <h1>Welcome to MDR</h1>
        <p>A monochrome design system for modern applications.</p>
      </main>
    </div>
  );
}
```

### 主题切换
```tsx
import { useTheme } from '@mdr/ui';

function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  
  return (
    <button 
      onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}
      className="theme-toggle"
    >
      {theme === 'light' ? '🌙' : '☀️'}
    </button>
  );
}
```

## 决策结论

选择 **单色设计系统** 作为 MDR Front Engine 的 UI 设计方向，基于以下原因：

### ✅ 优势
1. **专注内容** - 减少视觉干扰，突出核心功能
2. **现代简约** - 符合当代设计趋势
3. **开发友好** - CSS 变量系统，易于维护
4. **可扩展** - 灵活的颜色和组件系统
5. **可访问** - 良好的对比度和可读性

### ⚠️ 注意事项
1. **品牌识别** - 需要在简约和品牌特色间平衡
2. **用户接受度** - 部分用户可能偏好更丰富的色彩
3. **设计挑战** - 需要通过其他方式增加视觉层次

## 后续计划

### 第一阶段：基础系统（2 周）
- [ ] 完善颜色系统
- [ ] 建立间距和排版规范
- [ ] 开发基础组件（Button、Input、Card）
- [ ] 文档和示例

### 第二阶段：组件库（3 周）
- [ ] 复杂组件开发（Table、Modal、Form）
- [ ] 布局组件系统
- [ ] 主题切换功能
- [ ] 响应式设计

### 第三阶段：高级功能（2 周）
- [ ] 动画和过渡效果
- [ ] 无障碍功能完善
- [ ] 性能优化
- [ ] 开发者工具

### 第四阶段：集成应用（1 周）
- [ ] 与现有项目集成
- [ ] 使用指南编写
- [ ] 最佳实践总结
- [ ] 社区反馈收集

## 参考资料

- [Material Design](https://material.io/design)
- [Ant Design](https://ant.design/docs/spec/colors)
- [Tailwind CSS](https://tailwindcss.com/docs/customizing-colors)
- [单色设计原则](https://www.smashingmagazine.com/2016/04/monochromatic-color-schemes-in-web-design/)