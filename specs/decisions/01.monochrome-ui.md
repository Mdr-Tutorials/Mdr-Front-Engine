# [作废] 单色 UI 设计系统

## 状态

- Superseded
- 日期：2026-02-08
- 替代：`packages/themes/src/variables.scss`

## 决策说明

本决策文档作废，不再作为 UI 颜色与主题变量的依据。

当前项目的主题变量、明暗色板、语义色、间距、圆角、字号、阴影与过渡，统一以 `packages/themes/src/variables.scss` 为准。

## 执行要求

1. 新增或调整主题 token 时，仅修改 `packages/themes/src/variables.scss`。
2. 设计评审与实现评审均以 `packages/themes/src/variables.scss` 为单一真源。
3. 本文档仅保留为历史记录，不再维护。
